# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project state

Argus is a personal portfolio-intelligence system (digest + trade journal + quant + fundamental-analysis dossiers) for a single user, Omar. **The MVP (Phases 0–2) is largely built and deployed** — see the per-phase state below. The specs remain the source of truth for *what to build*; for *what is already built*, trust `git log`, never a prose snapshot (this section included — refresh it against the log when it drifts).

- `argus-blueprint.md` (v2.0, FINAL) — master spec. The single source of truth for everything in Phases 0–4.
- `argus-analyst-module.md` (v1.0) — the Phase 5 fundamental-analysis module. Independently buildable; built **after** the MVP.

**Current state (as of 2026-07-11 — verify against `git log`, not this prose):**
- **Phase 0 (spine) — done.** 16-table schema applied to the live DB; wrapped fetchers (`shared/fetcher_base.py`), Supabase client (`shared/db.py`), `fetch_log` writer (`shared/fetch_logger.py`); ingestion for Tiingo, FRED, IBKR Flex, Alpha Vantage / MarketWatch / Reddit news, locally-computed indicators; seeds for instruments, prices, calendar, and `config` (gates seeded + verified against a raw read-back).
- **Phase 1 (digest + bot) — done.** Full pipeline (`digest/`: fetch → URL dedup → Haiku sentiment → local indicators → bundle → Sonnet 5-clause synthesis → numeric grounding gate (`digest/grounding.py`, Law 2 enforced pre-store/pre-send) → store + frozen `bundle_json` → Telegram), plus `--dry-run`. The Vercel webhook (`api/webhook.py`) with fail-closed secret + chat-id auth (`bot/webhook_auth.py`); bot handlers `/book /journal /skip /health /override /pulse /felt` (`bot/handlers.py`); GitHub Actions `daily.yml` (two independent jobs: prices, journal), `digest.yml`, `event_filter.yml` — every scheduled workflow ends with an `if: failure()` Telegram alert (`bot/job_alert.py`).
- **Phase 2 (journal) — done, awaiting the operator.** Round-trip pairing, checkpoint engine + proximity/verdict push (`push_log` fire-once), in-the-moment `/felt` annotations + reconcile, **and the sleeve-entry writer** (`journal/sleeve_open.py`, 2026-07-10): interactive-only (refuses non-TTY — no job can register a sleeve), derives `floor(sleeve_pct × PV ÷ price)` from stored rows (cash DERIVED from contributions − buys − fees + sells), full refusal matrix (already-registered / missing `config.min_sleeve_shares` fee-dominance floor / inputs staler than 4 calendar days / a fill predating every stored contribution = the pre-widen Flex hole), writes the single `sleeve_shares` key only on a typed REGISTER. The floor is seeded explicitly (`python -m journal.seed_sleeve_floor N` — Omar's decision, no default). **Running the writer is Omar's act alone (L1) — Argus never suggests it.**
- **Phase 5 (analyst) — COMPLETE end to end (P1–P4, completion run 2026-07-06/07).** SEC company-facts mapper (12 concepts incl. capex + D&A, `ingestion/sec_facts.py`, multi-issuer with a symbol↔CIK pairing gate) → `fundamentals`/`fundamentals_latest`; split ledger + filed-date split reads (`quant/splits.py`); read-time metrics (`quant/metrics.py`). **Data-pack layer (`analyst/`):** ticker→CIK (`cik.py`), peer discovery (config `peer_overrides` > Finnhub), EDGAR 10-K/DEF 14A section extraction (`filings.py`), yfinance estimates (`estimates.py`), self-healing any-ticker ingestion (`ingest.py`, target AND peers), frozen-pack builder (`data_pack.py`; reduced-depth shape for non-SEC filers — `_filings_health` guards the string-note shape). **Valuation engine (`quant/valuation.py`):** 3-scenario discounted range, sensitivity, base-rate flag, bear-weighted MoS, reverse-DCF; grid in `config.valuation_assumptions`. **Dossier engine (P3, `analyst/dossier.py` + `law1.py` + `serialize.py`):** 8-stage / 5-clause synthesis over the serialized pack (the block IS the grounding whitelist), config-driven model (`config.synthesis_model`, seeded `claude-sonnet-4-6`; the seeder refuses to overwrite a changed row), ONE bounded repair pass (draft + violations fed back, minimal-edit instruction — a regenerate-style repair *introduced* violations), then FOUR gates pre-store: Law-1 lint (instruction shapes; `analyst/law1.py`), numeric grounding vs the block (stage refs + SEC form names masked as nomenclature), **claims-lint** (grounded-but-wrong superlatives; `analyst/claims.py` — the SERIES EXTREMA block pre-computes each series' true peak/trough and the gate enforces that a "peaked/highest/lowest" claim cites the actual extremum, not a recent salient value; caught the "EPS peaked at 3.61" class), and verdict-consistency (Graham MoS must be the valuation's own figure or null when n/m; ruin-list strings run the Law-1 patterns). Store to `analyses` with frozen pack+valuation, then `--send` delivers via Telegram (plain text, 4096-split). **P4:** `/analyze <TICKER>` in the webhook (shape-gate, BRK.B→BRK-B, instant ack) → `workflow_dispatch` on `analyze.yml`. Live rows: TSLA (EXPENSIVE / Mediocre@Premium / FRAGILE, MoS n/m), GM (CHEAP 69.4% MoS / Mediocre@Discount / FRAGILE), PLTR (EXPENSIVE / **Wonderful**@Premium / FRAGILE) — the §6 glamour/value disagreement plus the business-quality axis. The thousands-filer limitation is RESOLVED by denominational closure (2026-07-10): table-declaring filings sections contribute unit-expanded figures to the grounding whitelist (`validate_dossier_grounding`, derived purely from the frozen pack), and the shared gate's suffix vocabulary accepts the "X in thousands/millions" form — both directions of re-denomination now ground iff the arithmetic traces to a printed figure. `analyst`/`sec_facts`/`config_read` are NON_DATA_SOURCES (dossier failures surface via the red run + alert, not §5). **Dossier v2 (2026-07-12, Law-1 amendment scoped to the dossier):** a deterministic **bottom-line rating** (ATTRACTIVE / MIXED / UNATTRACTIVE "at current price", `analyst/rating.py`) is mapped from the three lens verdicts and *injected after* all four gates — so the recommendation-shaped sentence never reaches the Law-1 lint (which still blocks model-written buy/sell prose); it is stored in `analyses.verdicts` as `rating` + `rating_basis`. **Brief mode** (`config.dossier_length ∈ {brief, full}`, seeded `brief`, fail-loud read) shapes Telegram delivery only — the full dossier is always synthesized, gated and stored; `/analyze TICKER full` (or `--length full`) delivers the stored full text. The brief (~600–900 words) is a deterministic transform of the gated full text plus code-rendered numeric compacts (grounded-by-construction from the frozen pack). A **6th synthesis clause** + serializer glosses render plain language (terms of art glossed on first use, human-spoken figures, "basis points" banned for percentage points). Live-verified: TSLA brief delivered (rating UNATTRACTIVE, 741 words); mapper over the stored trio gives TSLA→UNATTRACTIVE, GM→MIXED, PLTR→UNATTRACTIVE. **Rating mapper v2 (2026-07-13, `analyst/rating.py`):** a growth-aware **regime gate** runs before the lens rules. A company whose realized 3y revenue CAGR ≥ `config.rating_growth_cagr_min` (0.15) is in the GROWTH regime — Graham (who tags every fast grower "EXPENSIVE") is demoted to context, and the rating turns on the reverse-DCF **gap ratio** = implied growth ÷ max(actual 3y CAGR, 2% floor): gap ≥ `rating_gap_extreme` (3.0) → UNATTRACTIVE; gap ≤ `rating_gap_ok` (1.5) + quality Wonderful/Good + not FRAGILE → ATTRACTIVE; else MIXED. Below the threshold it is the VALUE regime — v1 lens rules unchanged, Graham keeps his veto. Regime + CAGR + gap recorded in `rating_basis`; the render states the regime ("Rated on growth-adjusted basis…" / "Rated on value basis."). Live re-run of the stored trio: TSLA UNATTRACTIVE (value regime — 5.2% CAGR, Graham veto; the spec's gap-expectation didn't apply since TSLA is a slow grower), GM MIXED (value regime), **PLTR UNATTRACTIVE via the growth gap** (32.9% actual vs 144% implied ≈ 4.4× — "expensive for the right reason," not Graham's reflex).
- **Not built:** Phase 3 (quant models).
- **Signal Lab (Law 1 Amendment #2, 2026-07-13) — code COMPLETE, awaiting the operator.** A pre-registered EXPERIMENTAL timing signal (`siglab/`): a FAVORABLE/UNFAVORABLE rule (TSLA close > SMA50 AND MACD hist rising AND event filter clear next session AND VIX pctile < 80), computed at close D−1, SHADOW-scored against day D by a conservative mechanical bracket (both bands touched → loss, since EOD data can't order intraday fills). Registered immutably in `config.signal_v1` (rule + params + gates + registered_at, seeded 2026-07-13); the ledger lives in **`signal_ledger` (DDL proposed, `supabase/migrations/20260713120000_signal_ledger.sql` — Omar applies)**. Pure cores: `rule`/`shadow`/`engine`/`ledger`/`render`; DB wrapper `job` (backfill report + nightly `run_nightly`, insert-only-missing so a re-run never overwrites a live-scored day). **Verdict gates mirror the journal (L6, monotonic/terminal):** at N=30 triggered days, winrate<0.55 OR cum shadow P&L≤0 → RETIRED (permanent); at N=60, winrate≥0.58 AND P&L>fee×60×0.5 → PASS (unlocks a promotion decision that is **Omar's alone** — the code never executes/recommends). Rendered on `/today`, `/signal`, and the Monday digest with a mandatory `🧪 experiment` label; a hard render test bans buy/sell/enter/exit/should/good-day. **First honest read (read-only backfill over 170 stored days):** 38 FAVORABLE-triggered, **4 win / 34 loss (10.5%), cum shadow P&L −$841 → RETIRED**. The record is dominated by the conservative both-bands rule (28 of 38 losses) — TSLA's median daily range on those days was $13.76 vs a $3-wide bracket, so both bands are hit almost every day and score as losses; the shadow record is an explicit LOWER bound (a real intraday sequence could flip some). The gate retired v1 on schedule — the Lab working as designed, before a single real trade. A less-conservative or wider-bracket rule would be a fresh `signal_v2` ledger. **Package is named `siglab/` NOT `signal/` — a top-level `signal/` shadows Python's stdlib `signal` module and breaks third-party imports (verified); never rename it back.** Live writes await Omar applying the DDL then `python -m siglab.job --write`.
- **Funding landed — twice.** First real fills 2026-06-26 (TSLA 2 sh + SPCX 1 sh, both auto-classified `dca_buy`); `daily.yml` hard-fails Flex (Law 7). A **~$XXk deposit settled 2026-07-09** and is captured in `contributions` by the normal LastBusinessDay pull. The live Flex query emits **dd/MM/yyyy** dates (parsed since 2026-07-05). **Open backfill (Omar, portal-side, still owed as of 2026-07-10):** temporarily widen the Flex query period (e.g. Last 30 Calendar Days), say "re-pull" (the one-shot script retrieves ONCE, recovers the original June deposit into `contributions`, and applies the pre-authorized `exec_time` repair to transactions ids 3/4 with before/after states), then restore LastBusinessDay. The sleeve writer REFUSES to register until this lands (a fill predating every stored contribution proves derived cash is understated).

## Non-negotiable invariants (the 8 Operating Laws, as code rules)

These are unusual constraints that govern every line of code. Violating one is a correctness bug, not a style nit. Full text in blueprint §1.

1. **Information, never instruction.** No code path may emit buy/sell/"safe-to-trade"/timing/sizing language. Every synthesis prompt carries an explicit no-recommendation clause. (The Phase 5 analyst module may render *framework verdicts* — cheap/expensive, robust/fragile — but still never "buy now", "enter at $X", or "put 30% in".) **Amended 2026-07-12 (Omar), analyst dossier only:** the dossier additionally renders a deterministic, framework-derived **bottom-line rating** (ATTRACTIVE / MIXED / UNATTRACTIVE "at current price" — analyst-module §2), computed by code (`analyst/rating.py`) from the three lens verdicts and *injected after* the Law-1 lint (so the gate never whitelists free-form recommendation prose). Timing and sizing stay excluded even in the dossier; the digest, sleeve, and journal surfaces remain fully L1 (no recommendation language anywhere). **Amended 2026-07-13 (Omar), Signal Lab:** Argus renders a pre-registered experimental **timing signal** (`signal/`) — a FAVORABLE/UNFAVORABLE state plus a mandatory `🧪 experiment` label and live shadow track record. Signals are hypotheses under test, never unlabeled instructions: the render states the condition and the record and nothing else (no buy/sell/enter/exit/should/good-day — enforced by a hard render test), shadow P&L never touches real money, and the ledger is scored against pre-registered verdict gates. **Promotion of a signal into the actual trading strategy requires a PASS verdict at its pre-registered gates AND an explicit recorded decision by Omar** — the code renders evidence, never executes or recommends.
2. **Facts are retrieved, never generated.** The LLM synthesizes prose; it never supplies a date or number from memory. Every figure in any output must render from a stored DB row. Synthesis runs on a frozen input bundle, and each digest persists its exact `bundle_json` (each analysis its `data_pack_json`) so outputs are reproducible forever. If a fact isn't in the DB, the output says "not available" — it never fills the gap.
3. **Free-tier only.** Every external input must be free tier; adding any paid input is a separate explicit decision, never a silent code change. **X/Twitter is permanently excluded.** Respect per-source budgets — Alpha Vantage's 25 req/day is reserved 100% for news sentiment.
4. **Preparedness over prediction.** The forward calendar is first-class and rendered from `calendar_events` — never hallucinated. No single-point forecasts anywhere; forward views are scenario ranges with explicit assumptions.
5. **The core is untouchable.** Core holdings + all DCA contributions never enter the sleeve. This isolation is what makes the sleeve-only Δshares metric 100% trade-attributable — never write logic that mingles DCA buys into sleeve accounting.
6. **The journal is the verdict.** Gate metrics are pre-registered in `config` before trade #1; skipped trades are logged to `skip_log`; gates are never reinterpreted post-hoc. **Phase 2 (journal) must ship before the first round trip.**
7. **Silent failure is misinformation.** Every fetcher is wrapped; every failure is logged to `fetch_log` *and* surfaced in output (Source Health line, staleness flags, critical push alerts). Never swallow an exception that hides missing data.
8. **Boring beats clever.** Deterministic pipeline for v1. No agentic / LLM-driven control flow until Phase 4 at the earliest. Prefer the simplest thing that works.

## Architecture: three surfaces, one spine

The database is the product; every Telegram message is a view of it. Architecture diagram in blueprint §3, hosting rationale in §2 item 11.

- **Supabase Postgres — the spine.** All state lives here. Schema in blueprint §4. Read it directly from the bot; write to it from the pipeline.
- **Vercel (Python serverless) — the webhook ear.** Handles the Telegram webhook. Instant commands (`/book`, `/journal`, `/skip`, `/health`, `/override`) read Supabase directly and reply in ~1s. **Do no heavy work here** — that cold-start lag is the exact failure mode this topology was chosen to kill. `/pulse` and `/analyze` reply with an instant "Generating ⏳" ack and trigger GitHub Actions via `workflow_dispatch`.
- **GitHub Actions — all scheduled and heavy jobs.** The pipeline, Flex pulls, trade detection, event-filter checks, dossier runs. Schedules:
  - **Mon 11:00 UTC** — full weekly digest
  - **Daily 20:30 UTC** (weekdays) — IBKR Flex + prices + trade detection
  - **Daily 12:30 UTC** — event-filter check → morning warning push (15:30 Amman)
  - **workflow_dispatch** — `/pulse` (light pipeline) and `/analyze` (Phase 5 dossier)

**The pipeline (one engine):** `fetch (wrapped) → store raw → URL dedup → Haiku sentiment scoring → compute indicators locally (pandas_ta) → rank vs book → deltas vs last digest → Sonnet synthesis (5-clause contract) → store digest (+ bundle_json) → Telegram`.

## Critical design decisions (easy to get wrong)

- **Trade classification is by quantity proximity.** Sleeve round-trips are ~17 shares; DCA buys are ~0.6–2 shares. `transactions.trade_type` is auto-assigned; `transactions.override_type` (nullable, set via `/override`) **always wins**. Round-trip pairing: same-day sell of qty ≥ `0.8 × sleeve_shares` followed by a similar-qty rebuy.
- **The core metric is sleeve-only Δshares** (more shares = winning) — direction-neutral and contribution-proof. Pre-registered gates live in `config`: trade 10 (early warning), 20 (checkpoint → Phase B), 50 (verdict). See blueprint §8.
- **`config` is JSONB rows + `updated_at`, not schema.** Parameter changes (sleeve_pct, bracket, phase, weekly cap, watchlist) are config-row edits — never migrations, never hardcoded constants. Read these values from `config` at runtime. **Applied-DDL caveat:** `config` is keyed `key text primary key`, so updating a parameter *overwrites* its single row (refreshing `updated_at`) — it does NOT keep in-DB history. Historical auditability of the gates/params therefore rests on their being pinned in the git-tracked blueprint + seed (Law 6), not on config row-versions; a `config_history` table/trigger is deferred. **Re-seed hazard:** `ingestion/seed_config.py` upserts *all* config keys at once, so a full re-run against a live DB silently reverts any drift — an advanced `phase`, a split-adjusted `sleeve_shares` — back to the seed defaults (an L6 corruption). **Never full-re-seed a live DB**; use single-key upserts for one-key changes. The guard is now built: `seed_config` REFUSES a full run once any config row exists, and `--missing-only` upserts only absent seed keys (there is deliberately no `--force`).
- **Sleeve symbol is config-driven and fail-loud.** The single sleeve ticker lives in `config.sleeve_symbol`, resolved at runtime — the twice-defined `_SLEEVE_SYMBOL = "TSLA"` constant (in `bot/handlers.py` and `journal/checkpoint.py`) is retired, and the single config row is the source of truth, closing the cross-surface drift footgun. Resolution **refuses to guess** on a missing/blank/non-string row: the `/felt` webhook returns a refusal without staging a note, the checkpoint engine raises (surfaced via `fetch_log`, Law 7). A wrong ticker is a corrupt journal row / wrong Δshares divisor (Law 6), strictly worse than the old constant — so there is deliberately **no default** (unlike numeric params like `sleeve_shares`, which may fall back). Resolution is **module-local** (`handlers._sleeve_symbol`, `checkpoint._load_sleeve_symbol`) by design; a shared `shared/config` read-helper unification is a separate future effort, not bolted on here. (Ship status: trust `git log`.)
- **Young-ticker indicator suppression.** SPCX listed 2026-06-12 with almost no history. `instruments.first_trade_date` drives suppression — do not compute or display an indicator that lacks enough history (no SMA50 until 50 sessions exist, etc.).
- **`sleeve_shares` is derived, never hardcoded.** At sleeve entry, `sleeve_shares = floor(sleeve_pct × live_portfolio_value ÷ TSLA_price)` (portfolio value = Flex positions market_value + cash). It is then frozen as the registered unit through the 50-trade verdict and re-derived only at a phase gate — this keeps the absolute Δshares thresholds (−1.0, < 0) meaningful (L6). `config` does not seed a `sleeve_shares` constant — it is unset until a sleeve opens (the stale illustration row `17` was deleted from the live DB 2026-07-01); the read path treats "unset" as no active sleeve (graceful), while code that requires it fails loud. Corporate Actions auto-adjust the frozen count on splits (the split ledger + read layer exist; the auto-adjust write fires only once a sleeve is active).
- **SPCX conditional unlock is monitored from `prices_eod`:** close ≥ $175.50 on ≥5 of 10 sessions post-Q2-earnings arms it. SPCX calendar seed in blueprint §14.

## Source & reliability contract

Every external call is wrapped (this is Law 7 in code): **30s timeout, 2 retries 30s apart, then mark unavailable** + add a footer/health line. Log `source / run_id / status / latency_ms / error` to `fetch_log` every run. Staleness: prices >2 trading days or Flex >48h → warn in digest. Critical alerts: Flex fails 2 days (journal goes blind) and Monday digest total failure (auto-retry once at +1h, then fail loud). Details in blueprint §12.

Sources (all free, blueprint §5): **Tiingo** (prices — TSLA/SPCX/SPY/QQQ), **FRED** (6 macro series), **IBKR Flex Web Service** (daily portfolio), **Alpha Vantage NEWS_SENTIMENT** (company news — entire 25/day budget), **Reuters RSS** (wire), **Reddit r/stocks RSS** (retail), **Finnhub → yfinance** (earnings dates), **yfinance** (universal fallback). Indicators are computed **locally** (pandas_ta) and validated against TradingView at build time. Dedup is URL-match only.

## Build phases & ordering rules

Phases (blueprint §10): **0 Spine** → **1 Digest + Bot** → **2 Journal** → **3 Quant** → **4 optional agentic add-on** → **5 Analyst module**.

- **Hard rule: Phase 2 (journal) ships before round-trip #1.** Gates must be pre-registered before any sleeve trade.
- **MVP = Phases 0–2** (~29–40 h). That is where the daily value is.
- **Do not build the Analyst module (Phase 5) first.** It shares the spine but does not block or depend on the MVP; build it on a working spine.

**Phase 0 scope:** Supabase schema (all §4 tables) + Tiingo/FRED/IBKR-Flex ingestion + 200-day historical price seed + SPCX calendar seed (§14) + `fetch_log` and the wrapped fetchers. IBKR Flex query + read-only token setup happens at Phase 0 build time.

## Tech stack & intended layout

Python throughout (Vercel Python serverless + GitHub Actions Python jobs). `pandas_ta` for indicators. Claude API: **Haiku** for sentiment scoring (behind a swappable `score_headlines()` interface) and **Sonnet** for synthesis. Supabase Postgres for state.

Monorepo layout (blueprint §2 item 14) — **scaffolded, nothing implemented yet**: the packages `ingestion/ digest/ journal/ bot/ quant/ shared/` each exist with a docstring-only `__init__.py`, and `.github/workflows/` holds a `.gitkeep` placeholder (workflows arrive in Phase 1).

## Commands

- **Tests:** `.venv\Scripts\python.exe -m pytest -q` from the repo root (pure-logic unit tests; no DB/network). No linter is configured yet.
- **Domain probes (read-only, run against the live DB):** `python -m quant.splits` (adjusted shares series), `python -m quant.metrics` (TSLA metrics table + gross-margin identity), `python -m digest.grounding [digest_id]` (validate a stored digest against its frozen bundle), `python -m digest.pipeline --run-type monday --dry-run` (freeze a bundle, no spend), `python -m analyst.data_pack TSLA` (build + summarize a frozen pack; writes gitignored `_datapack_dryrun.json`), `python -m analyst.filings TSLA` / `python -m analyst.cik TSLA GM` (section extraction / CIK resolution probes).
- **Dossier runs (SPEND ~$0.30–0.60/run — Sonnet synthesis):** `python -m analyst.dossier TSLA --print-only` (synthesize + all four gates, NO store — the prompt-iteration loop); `python -m analyst.dossier TSLA` (store to `analyses` on gate pass); `--send` additionally delivers to Telegram (the analyze.yml step).
- **Seeds:** `python -m ingestion.seed_corporate_actions` (idempotent); `python -m ingestion.seed_config` (bootstrap only — refuses on a live config; `--missing-only` adds absent keys); `python -m analyst.seed_peers` (single-key `peer_overrides` upsert + peer instruments rows — never a full config seed); `python -m analyst.seed_synthesis_model` (single-key; refuses to overwrite a deliberately changed row); `python -m analyst.seed_dossier_length [brief|full]` (single-key `dossier_length`, seeds `brief` — the Telegram delivery length; full is always stored); `python -m analyst.seed_rating_config` (three single-key upserts — `rating_growth_cagr_min` 0.15 / `rating_gap_extreme` 3.0 / `rating_gap_ok` 1.5, the mapper-v2 regime + gap thresholds; absent → seed default, present-but-corrupt → fail loud); `python -m siglab.seed_signal` (registers the IMMUTABLE `config.signal_v1` blob — rule + params + verdict gates; refuses to overwrite once registered, L6; an edit is a new signal_v2, never a rewrite); `python -m journal.seed_sleeve_floor N` (single-key `min_sleeve_shares` — the fee-dominance floor, Omar's explicit value, no default); `python -m ingestion.seed_target_usd [amount]` (single-key `target_usd`, the $100K goal, default 100000).
- **Sleeve registration (interactive-only, writes on typed REGISTER):** `python -m journal.sleeve_open` — refuses from jobs/pipes; the one deliberate human action that arms the journal.
- **Domain CLI (spec'd, not yet implemented):** `seed-calendar --year YYYY` — guided annual seeding of FOMC/CPI/NFP dates.
- **Telegram commands:** `/today` `/signal` `/pulse` `/analyze` `/book` `/journal` `/felt` `/skip` `/health` `/override`. **`/signal`** (2026-07-13) renders the Signal Lab full ledger (rule, record, gate progress); the labelled one-liner also rides in `/today` and the Monday digest Your-Book block. **`/today`** (2026-07-13) is a deterministic, LLM-free trade-context card (`bot/handlers.handle_today`): per-watchlist trend (price vs 50/200-day average, categorical) + momentum (RSI glossed "momentum score, 50 neutral"; MACD-vs-signal as improving/fading), §8 event-filter state (shared arm decision), round trips this week vs cap, sleeve registration. Grounding-exempt (pure DB render) but hard-L1: it describes, never advises — `tests/test_today_handler.py::test_card_never_advises` bans buy/sell/enter/exit/should/could/good-bad-day language.

## Cost guardrail

Target is ~$1–2/month total (data $0, hosting $0 on free tiers, Claude API the only spend). Sonnet digest ~$0.07–0.15/run, Haiku scoring ~$0.01–0.03/run. A change that materially raises this needs an explicit decision (Law 3).
