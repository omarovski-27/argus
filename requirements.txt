# Vercel webhook function — LEAN runtime deps only (fast cold start, under the function-size cap).
# This is the webhook's whole import closure; postgrest rides along with supabase.
# The GitHub Actions jobs install the FULL set from requirements-jobs.txt
# (pandas / pandas-ta / anthropic for ingestion + digest) — keep the two in sync deliberately.
supabase
httpx
python-dotenv
